/**
 * Verbatim source excerpts from my Deep Research Agent, reproduced exactly as
 * they appear in the repository so the panels in the UI are real evidence and
 * not a paraphrase. Line ranges are recorded alongside each excerpt in
 * `lib/engineering.ts`.
 *
 * Generated from the source files — do not hand-edit the strings.
 */

export const source = {
  blueprint: "class OutlineSection(BaseModel):\n    title: str\n    objective: str\n    key_points: list[str]\n    research_needed: list[str]\n    examples_needed: list[str]\n\nclass ResearchTasks(BaseModel):\n    concepts: list[str]\n    frameworks: list[str]\n    examples: list[str]\n    case_studies: list[str]\n    statistics: list[str]\n\n\nclass ResearchBlueprint(BaseModel):\n    \"\"\"Structured blueprint for the research phase.\"\"\"\n\n    metadata: Metadata\n\n    executive_intent: ExecutiveIntent\n\n    outline: list[OutlineSection]\n\n    research_tasks: ResearchTasks\n\n    constraints: list[str]\n\n    assumptions: list[str]\n\n    success_criteria: list[str]\n\n    approval_status: Literal[\"draft\"] = Field(\n    default=\"draft\",\n    description=\"Blueprint generated by the Scoping Agent. Approval is handled externally by n8n.\"\n)\n    version: int = Field(\n    default=1,\n    description=\"Blueprint version\"\n)",
  clarify: "def clarify_with_user(state: AgentState) -> Command[Literal[\"generate_research_blueprint\", \"__end__\"]]:\n\n    # Skip clarification during blueprint updates.\n    # The existing blueprint already contains the required scope.\n    if state.workflow.operation == \"update\" and state.research_blueprint is not None:\n        return Command(\n            goto=\"generate_research_blueprint\"\n        )\n\n    structured_output_model = model.with_structured_output(ClarifyWithUser)\n\n    response = structured_output_model.invoke([\n        HumanMessage(\n            content=clarify_with_user_instructions.format(\n                messages=get_buffer_string(messages=state[\"messages\"]),\n                date=get_today_str()\n            )\n        )\n    ])\n\n    # Route based on clarification need\n    if response.need_clarification:\n        return Command(\n            goto=END, \n            update={\"messages\": [AIMessage(content=response.question)]}\n        )\n    else:\n        return Command(\n            goto=\"generate_research_blueprint\", \n            update={\"messages\": [AIMessage(content=response.verification)]}\n        )",
  approval: "    # Approval message\n    markdown += \"\"\"\n\n## Review Required\nPlease review the Research Blueprint.\nYou may choose one of the following actions:\n- Approve \u2013 Accept the blueprint and begin research.\n- Update \u2013 Request modifications to the blueprint.\n- Reject \u2013 Discard this blueprint and end the workflow.\n\"\"\"\n    return {\n        \"messages\": [\n            AIMessage(content=markdown)\n        ]\n    }",
  parallel: "            # Handle ConductResearch calls (asynchronous)\n            if conduct_research_calls:\n                # Launch parallel research agents\n                coros = [\n                    researcher_agent.ainvoke({\n                        \"researcher_messages\": [\n                            HumanMessage(content=tool_call[\"args\"][\"research_topic\"])\n                        ],\n                        \"research_topic\": tool_call[\"args\"][\"research_topic\"]\n                    }) \n                    for tool_call in conduct_research_calls\n                ]\n\n                # Wait for all research to complete\n                tool_results = await asyncio.gather(*coros)\n\n                # Format research results as tool messages\n                # Each sub-agent returns compressed research findings in result[\"compressed_research\"]\n                # We write this compressed research as the content of a ToolMessage, which allows\n                # the supervisor to later retrieve these findings via get_notes_from_tool_calls()\n                research_tool_messages = [\n                    ToolMessage(\n                        content=result.get(\"compressed_research\", \"Error synthesizing research report\"),\n                        name=tool_call[\"name\"],\n                        tool_call_id=tool_call[\"id\"]\n                    ) for result, tool_call in zip(tool_results, conduct_research_calls)\n                ]\n\n                tool_messages.extend(research_tool_messages)\n\n                # Aggregate raw notes from all research\n                all_raw_notes = [\n                    \"\\n\".join(result.get(\"raw_notes\", [])) \n                    for result in tool_results\n                ]",
  guardrails: "    # Check exit criteria first\n    exceeded_iterations = research_iterations >= max_researcher_iterations\n    no_tool_calls = not most_recent_message.tool_calls\n    research_complete = any(\n        tool_call[\"name\"] == \"ResearchComplete\" \n        for tool_call in most_recent_message.tool_calls\n    )\n\n    if exceeded_iterations or no_tool_calls or research_complete:\n        should_end = True\n        next_step = END",
  think: "@tool(parse_docstring=True)\ndef think_tool(reflection: str) -> str:\n    \"\"\"Tool for strategic reflection on research progress and decision-making.\n\n    Use this tool after each search to analyze results and plan next steps systematically.\n    This creates a deliberate pause in the research workflow for quality decision-making.\n\n    When to use:\n    - After receiving search results: What key information did I find?\n    - Before deciding next steps: Do I have enough to answer comprehensively?\n    - When assessing research gaps: What specific information am I still missing?\n    - Before concluding research: Can I provide a complete answer now?\n\n    Reflection should address:\n    1. Analysis of current findings - What concrete information have I gathered?\n    2. Gap assessment - What crucial information is still missing?\n    3. Quality evaluation - Do I have sufficient evidence/examples for a good answer?\n    4. Strategic decision - Should I continue searching or provide my answer?\n\n    Args:\n        reflection: Your detailed reflection on research progress, findings, gaps, and next steps\n\n    Returns:\n        Confirmation that reflection was recorded for decision-making\n    \"\"\"\n    return f\"Reflection recorded: {reflection}\"",
} as const;
